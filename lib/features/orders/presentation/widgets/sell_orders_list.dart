import 'package:flutter/material.dart';

class SellOrdersList extends StatelessWidget {
  const SellOrdersList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("page 2"),
    );
  }
}
